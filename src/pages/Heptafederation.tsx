import { PageShell } from "@/components/PageShell";

const Heptafederation = () => (
  <PageShell title="Heptafederation">
    <div className="flex min-h-[60vh] items-center justify-center">
      <p className="text-lg text-muted-foreground">Heptafederation — próximamente</p>
    </div>
  </PageShell>
);
export default Heptafederation;
